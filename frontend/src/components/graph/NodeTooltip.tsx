// =============================================================================
// components/graph/NodeTooltip.tsx
// =============================================================================
//
// ROLE
// ----
// Hover tooltip for graph nodes. Surfaces host metadata + current risk +
// quick links to /hosts/{id} and /flows?host={id}.
//
// IMPL NOTES
// ----------
//   - Position computed from Cytoscape's renderedPosition,
//   - Rendered as a portal so it escapes the canvas clipping,
//   - 80ms entrance fade for non-jarring hover behavior.
// =============================================================================

export function NodeTooltip() {
  return null; // architecture skeleton
}
