// =============================================================================
// components/hosts/HostProfileCard.tsx
// =============================================================================
//
// ROLE
// ----
// Detailed profile for the currently selected host: hostname, IPs, subnet,
// OS guess, first/last seen, current risk, count of related alerts/paths.
//
// DATA
// ----
//   - hostsService.profile(host_id) — lazy fetch on selection.
// =============================================================================

export function HostProfileCard() {
  return <div className="glass p-5">{/* host metadata */}</div>;
}
