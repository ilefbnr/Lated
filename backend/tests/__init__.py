# =============================================================================
# LateD backend — test suite root
# =============================================================================
# Test modules cover one architectural module each. Per-module test files
# share fixtures via `conftest.py` (added at implementation time).
# =============================================================================
