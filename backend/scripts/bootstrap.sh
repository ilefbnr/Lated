#!/usr/bin/env bash
# =============================================================================
# scripts/bootstrap.sh — first-run setup
# =============================================================================
# Purpose:
#   Prepare a fresh dev/staging environment:
#     - create the data directories,
#     - run a passive discovery pass to seed the baseline,
#     - verify a signed TGNN artifact is present.
#
# Usage:
#   ./bootstrap.sh
# =============================================================================
set -euo pipefail
echo "[bootstrap] Architecture skeleton — implement once modules are real."
