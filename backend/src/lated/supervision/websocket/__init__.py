# =============================================================================
# lated.supervision.websocket — WebSocket subsystem
# =============================================================================
# Files:
#   ws_server.py        : the ASGI WebSocket route + connection manager
#   ws_channels.py      : channel routing (alerts, graph, hosts, flows, ...)
#   event_publisher.py  : internal publisher used by detection/correlation
# =============================================================================
